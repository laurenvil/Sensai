// blink.ino
// 💡 Module 02 — Your First Circuit
// YOUR TASK: Make the LED on pin 13 blink on and off every 500 ms.
//
// Instructions:
// 1. In setup(), set pin 13 as an OUTPUT using pinMode()
// 2. In loop(), turn the LED on with digitalWrite(), wait 500ms, turn off, wait 500ms

const int LED_PIN = 13;  // 💡 Always name your pins — never use magic numbers!

void setup() {
    // TODO: Set LED_PIN as OUTPUT
    // Hint: pinMode(LED_PIN, OUTPUT);
}

void loop() {
    // TODO: Turn LED on
    // Hint: digitalWrite(LED_PIN, HIGH);

    // TODO: Wait 500 milliseconds
    // Hint: delay(500);

    // TODO: Turn LED off
    // Hint: digitalWrite(LED_PIN, LOW);

    // TODO: Wait 500 milliseconds
    // Hint: delay(500);
}
